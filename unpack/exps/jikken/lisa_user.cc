#include "structures.hh"
#include <math.h>
#include <stdint.h>

void raw_user_function(unpack_event *event, raw_event *raw_event)
{
    /*for (int i = 0; i < 16; i++)
    {
        for (int j = 0; j < 10000; j++)
        {
            raw_event->TRACE[16] = event->trace.trace.ftrace[16];
        }
    }*/

   // raw_event->TRACE = event->trace.trace.ftrace;
        
}